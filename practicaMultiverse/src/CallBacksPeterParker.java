public interface CallBacksPeterParker {
    //Lo que hace
    void fuerzaSobrehumana(Screen s);
    void curacion(Screen s);
    void reflejosSobrehumana(Screen s);
    void trepaMuros(Screen s);
}
